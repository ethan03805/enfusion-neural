# Remaining acceptance

Target: faithful photorealistic appearance at 2560 × 1440 and 30–60 FPS on the RX 7800 XT in local single-player.

| Area | Established | Required next |
| --- | --- | --- |
| Live loop | Actual game capture → GPU neural pass → visible companion; source bypass, separate viewer and ten-minute mostly stationary runtime check | Physical WASD/mouse check, menu/focus/scope transitions and extended movement |
| Budget | Runtime-verified town baseline, individual reductions and enhancement; separate recording overhead; second matched 74-metre street comparison | Repeated passes and denser vegetation/interior coverage; displayed-frame telemetry remains a specific external limitation |
| Latency | 895-frame software display join; moving-path processing age | Moving-path capture-to-display evidence and physical input-to-photon test |
| Model | Native Zero-DCE++ parity; 3DLUT, REGEN, DeepLPF and SPAN evaluated; native material controls verified | A reference-based model or asset workflow with demonstrated material/lighting gains; another unrestricted photo grade alone does not satisfy this gate |
| Fidelity | Fixed source coordinates, bounded brightness and source fallback; normal-speed paths | Visibility/temporal acceptance around cover, openings, thin foliage and darker environments |
| Delivery | Launcher, companion, addon, attribution, normal-speed capture and measured evidence | Finish the explicit acceptance gaps before calling this the requested photorealistic prototype |

The next model task must state its actual available inputs and a bounded appearance test before implementation. Do not resume unchanged Blender studies, screenshot/widget probes or unsupported renderer-hook investigations. Preserve the working RGB companion and all failed evidence while changing the appearance approach.

The generic RGB checkpoint route has now produced either modest grading or identity/visibility failures. Before another integration, establish a usable reference for one existing road, roof or masonry asset and identify what correction can be learned from the available RGB. A controlled material pair must retain the actual asset identity; a new synthetic room is not acceptance for gameplay.

The replacement street route now passes the declared motion and repetition gates in standard/reduced/enhanced runs. Keep the failed fence pilots and world-loading crash. The subsequent DXGI statistics investigation is closed after its two declared probes: both return zero display counters, including the `DwmFlush` variant. Do not repeat those probes or the previous ETW search without new evidence that changes their feasibility.

The appearance investigation now verifies all 13 native prefab material defaults for `House_Village_E_1L02t`. Per-instance overrides remain unresolved, and the declared file-access probe times out without any copied texture data. Close this route and retain its evidence. Do not repeat it without new feasibility information.

SPAN passes its synthetic reconstruction comparison but exceeds the declared GPU-call budget in both precisions. Its bounded residual only sharpens the original. Close this graph's live integration and preserve all outputs, including the initial FP16 export failure. Further resolution metrics alone cannot satisfy material/lighting acceptance.

The two-view collision test is complete within its three-validation/two-capture bound. Integer-pixel unprojection passes the second control, but 2,304 rays cost 16–25 ms CPU and leave thin geometry, openings and shading normals unresolved. Preserve this offline diagnostic and close the per-frame grid route; do not add it to the live RGB contract.

The native material source/change/reset control passes on its first validation and capture sequence. Cached `RoughnessScale=0.05` produces a 26.14-code roof response; reset error is 0.04 codes in the declared region. Preserve the nonzero whole-frame difference and shared-material scope. The diagnostic sheen is not accepted dry-slate appearance.

The moderate 0.4 / 0.7 candidates are now rejected after three fixed-environment views and inspection of slate references. The original material retains better weathered contrast in the reserved view. Keep the API control and all evidence, but do not package a preset or use these images as improved targets.

Next bounded task: evaluate the author's Depth Anything V2 Small checkpoint from available RGB, at short-side input sizes 252 and 392. Allow 30 minutes and at most two static graph sizes, with one FP16 conversion if FP32 is too costly. Preserve aspect ratio and record actual tensor sizes. Use the two existing geometry captures for visual checks; use only the integer-coordinate street capture for quantitative comparisons against selected solid collision hits. Fit any relative-depth scale on one declared spatial subset and report a separate reserved subset. Do not treat foliage proxies as ground truth. Require finite output, CPU/GPU parity, useful depth ordering and declared RX 7800 XT cost before any lighting composition. Inferred depth still needs motion/visibility checks and does not itself demonstrate appearance improvement.

[Current status](status.md) · [Build and comparison](playable.md) · [Research archive](research-history.md)
