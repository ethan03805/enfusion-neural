# Remaining acceptance

Target: faithful photorealistic appearance at 2560 × 1440 and 30–60 FPS on the RX 7800 XT in local single-player.

| Area | Established | Required next |
| --- | --- | --- |
| Live loop | Actual game capture → GPU neural pass → visible companion; source bypass, separate viewer, three-minute repeated movement and ten-minute mostly stationary runtime check | Physical WASD/mouse check and menu/focus/scope transitions |
| Budget | Runtime-verified town baseline, individual reductions and enhancement; separate recording overhead; matched 74-metre street and 180-second repeated-route comparisons | Repeated passes and denser vegetation/interior coverage; displayed-frame telemetry remains a specific external limitation |
| Latency | 895-frame software display join; moving-path processing age | Moving-path capture-to-display evidence and physical input-to-photon test |
| Model | Native Zero-DCE++ parity; appearance models and native material controls evaluated; coarse RGB depth feasibility measured | Demonstrated material/lighting gain; another photo grade or depth map alone does not satisfy this gate |
| Fidelity | Fixed source coordinates, bounded brightness and source fallback; normal-speed paths; channel guard passes offline fixtures and current live snapshot | Visibility/temporal acceptance around cover, openings, thin foliage and darker environments |
| Delivery | Launcher, companion, addon, attribution, normal-speed capture and measured evidence | Finish the explicit acceptance gaps before calling this the requested photorealistic prototype |

The [paired exposure diagnostic](playable.md#paired-frame-exposure-stability) finds 51,129 newly clipped channel samples despite passing coarse temporal checks. Its CPU composition failure remains retained; the native alternative exactly reproduces the original snapshot. The subsequent [headroom correction](playable.md#channel-preserving-correction) closes after **16.8 minutes**, passing all 600 frames, six fixed curve fixtures and unchanged coarse temporal limits. Source positions, model, strength and protected pixels remain fixed. Full perceptual and visibility acceptance remain open.

The separate [live release check](playable.md#guarded-build-verified-in-gameplay) closes in **17.9 minutes**. Exact snapshot replay, independent CPU curves, clipping/protected-pixel limits, F8/F9/F10 and both application FPS/p95 gates pass. The updated ZIP includes the tested binary; the old ZIP remains available. All 1,855 recording timestamps are preserved. One failed VFR review extraction and its corrected tooling remain recorded. This correction does not resolve the missing appearance target or substantial photorealistic gain.

Next is a **20-minute menu and focus transition check** using the exact extracted release package in one private town session. Freeze package/model/addon hashes and the interaction sequence before launch. Observe the game menu open/close, F8 while the menu is open, focus transfer to the existing Codex window and return, then F10 with the game continuing. Use supported UI tools and fresh observed windows. Retain screenshots, native events, mode changes and timestamps. Require source fallback during loss of focus, successful fresh-frame resumption after return, usable original menu through bypass, clean exit and no crash. Report observed transition bounds separately from any physical/display latency; do not infer key sampling from automation.

Keep the existing cadence evidence; do not run another baseline or alter the shader/model in this transition check. A failure retains working code and a specific reproduction, with no blind retries beyond the bound. Physical WASD/mouse acceptance stays pending unless the user actually reports it. Moving-path display latency stays unavailable; unchanged ETW/DXGI probes remain closed. Full appearance and semantic acceptance require an aligned target or new feasibility evidence, not additional exposure grades or more copies of the same performance run.

Earlier results and failed attempts remain in their linked evidence. The [sustained comparison](playable.md#three-minutes-of-repeated-movement) passes its final movement/path gates; review closes at 66.0 minutes, beyond its original hour. The rejected synthetic proposal closes in 3.5 minutes and supplies no accepted aligned target. Do not train from it or infer substantial appearance progress from these independent runtime and stability checks.

Closed routes remain available in the [appearance evidence](model-evaluation.md), [playable work log](playable.md) and [decisions](decisions.md):

| Route | Reason to keep it closed |
| --- | --- |
| Unchanged renderer bridge, screenshot/widget and Blender investigations | No verified live surface-buffer path; Blender is background research |
| Moving-path ETW / DXGI statistics probes | Empty or zero display counters, including the single DwmFlush variant |
| Native texture copying | Timed out without output; material identity lookup still works |
| Per-frame collision grid | 16–25 ms CPU and unresolved visible-surface correspondence; offline use remains valid |
| Rejected photo grades and SPAN live integration | Visibility/identity failure, inadequate appearance gain or excessive cost |
| Roof roughness 0.4 / 0.7 presets | Pale sheen and reduced slate contrast in the reserved view; retain the working change/reset control |
| Depth 392 FP32 / single FP16 graph | FP32 over budget; FP16 session requires CPU-assigned operations |
| Depth motion graph in its current Python workflow | 15.88 ms p95 model call plus 30.16 ms median CPU preprocessing; no demonstrated appearance benefit or reliable thin geometry |
| Cached sky material, SkyIntensityLV 8 → 8.5 | Assignment succeeds without a useful visible response; no accepted lighting target |
| IAT exposure checkpoint at the fixed RGB contract | 28–30 ms synchronized calls, lost shade detail; bounded output adds mask seams and insufficient appearance gain |
| Source-conditioned synthetic proposal | Reconstructed fine detail/text and smaller canvas; not an identity-preserving aligned target |

Do not retry these unchanged routes without new feasibility evidence. Preserve all failures, including fence-colliding paths, the world-loading crash and failed conversions. Physical input verification remains pending user interaction; it does not prevent bounded offline work.

[Current status](status.md) · [Build and comparison](playable.md) · [Research archive](research-history.md)
