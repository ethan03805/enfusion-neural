* text=auto eol=lf
# Preserve byte-for-byte model identity across Git checkouts.
models/*.json -text whitespace=cr-at-eol
models/lighting-diversity-v1/*.json -text whitespace=cr-at-eol
# Retain the exact declared plan bytes used by the native visibility controls.
scenes/arma-source-visibility-v1.json -text whitespace=cr-at-eol
# Preserve the exact pre-measurement plans and amendment recorded in the replay evidence.
scenes/playable-dce-temporal-v1.json -text whitespace=cr-at-eol
scenes/playable-dce-temporal-native-v2.json -text whitespace=cr-at-eol
